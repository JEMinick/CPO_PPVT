<div class="flex-column justify-space-around">
  <div class="display-flex justify-space-between align-center mb-4">
    <h3>My Profile</h3>
    <nav class="navbar navbar-dark bg-dark">
      <div class="icons">
        {{!-- <i class="fas fa-plus text-light">Add Pet</i> --}}
        <button class="btn btn-primary" 
                id="delprofilebtn" 
                type="submit">
                <a href="/api/users/del/{{loginid}}">
                  <i class="fas fa-trash text-light">  Delete Profile</i>
                </a>
        </button>
      </div>
    </nav>
  </div>
</div>

{{!-- {{#if petInfo.length}}
  <article>
    {{#each petInfo as |pet| }}
      {{> pet-info pet}}
    {{/each}}
  </article>
{{/if}} --}}

{{!-- <script src="/js/addPet.js"></script> --}}
